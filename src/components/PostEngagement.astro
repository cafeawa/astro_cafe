<div class="post-engagement" aria-label="文章互动数据">
	<span class="post-engagement__item" title="文章浏览量">
		<svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
			<path d="M2.5 12s3.5-6 9.5-6 9.5 6 9.5 6-3.5 6-9.5 6-9.5-6-9.5-6Z" />
			<circle cx="12" cy="12" r="2.5" />
		</svg>
		<span class="waline-pageview-count" aria-live="polite">—</span>
		<span>阅读</span>
	</span>

	<span class="post-engagement__divider" aria-hidden="true"></span>

	<a class="post-engagement__item post-engagement__link" href="#comments" title="跳到评论区">
		<svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
			<path d="M21 11.5a8.4 8.4 0 0 1-9 8.5 9.7 9.7 0 0 1-4-.8L3 21l1.5-4.2A8.5 8.5 0 1 1 21 11.5Z" />
		</svg>
		<span class="waline-comment-count" aria-live="polite">—</span>
		<span>评论</span>
	</a>
</div>

<style>
	.post-engagement {
		display: flex;
		align-items: center;
		justify-content: center;
		gap: 0.65rem;
		margin-top: -0.35rem;
		color: #64748b;
		font-size: 0.875rem;
		font-variant-numeric: tabular-nums;
	}

	.post-engagement__item {
		display: inline-flex;
		align-items: center;
		gap: 0.3rem;
		min-height: 2rem;
	}

	.post-engagement__item svg {
		width: 1rem;
		height: 1rem;
		color: var(--primary-500);
	}

	.post-engagement__divider {
		width: 1px;
		height: 0.85rem;
		background: #cbd5e1;
	}

	.post-engagement__link {
		border-radius: 9999px;
		color: inherit;
		text-decoration: none;
		transition: color 0.15s ease;
	}

	.post-engagement__link:hover,
	.post-engagement__link:focus-visible {
		color: var(--primary-700);
	}

	.post-engagement__link:focus-visible {
		outline: 2px solid var(--primary-300);
		outline-offset: 3px;
	}

	:global(.dark) .post-engagement {
		color: var(--color-zinc-400);
	}

	:global(.dark) .post-engagement__divider {
		background: var(--surface-line-strong);
	}

	:global(.dark) .post-engagement__link:hover,
	:global(.dark) .post-engagement__link:focus-visible {
		color: var(--primary-300);
	}
</style>
